# Infra

Esta carpeta se conserva porque el PDF del proyecto pide que el despliegue sea reproducible y documentado.

Uso previsto:

- configuraciones de Docker o Compose complementarias;
- scripts de despliegue o inicializacion;
- configuracion de proxy, servidor web o automatizaciones operativas.

Regla actual:

- si no existe infraestructura real todavia, mantener esta carpeta minima y documentada;
- evitar subcarpetas vacias que aparenten configuracion inexistente;
- agregar archivos aqui solo cuando representen infraestructura utilizable por el equipo.
